# Бриф на автоматизацію — [CLIENT_1]

> **Санітизована версія** `materials/client-brief.md` для роботи з AI.
> Справжні значення прибрано або замінено: імена й контакти — плейсхолдерами,
> доступи — `<SECRET_OUT_OF_BAND>`, вигрузка — синтетичними рядками, комерція —
> `<INTERNAL>`. Задача й критерії приймання збережені дослівно.
> Як це зроблено — [`sanitization-checklist.md`](./sanitization-checklist.md).

## Класифікація оригіналу (світлофор)

| Блок оригіналу | Що там було | Рівень | Техніка | У цьому документі |
|---|---|---|---|---|
| Заголовок | назва компанії-клієнта | 🟡 ідентичність клієнта | плейсхолдер | `[CLIENT_1]` |
| Контакт | ПІБ, робочий email, телефон, особистий месенджер | 🔴 PII | плейсхолдер · masking · redaction | `[CONTACT_1]`, `[CONTACT_1_EMAIL]`, `+380 (XX) XXX-XX-XX`, `<REMOVED>`; роль лишено |
| Задача й критерії | процес, обсяги, критерії приймання | 🟢 постановка (назви систем — 🟡) | без змін | дослівно, крім назви клієнта |
| Доступи | API-ключ, bot-токен, base ID + PAT, URL вебхука, connection string | 🔴 секрети | out-of-band | `<SECRET_OUT_OF_BAND>` + назва змінної |
| Вигрузка з продакшену | ПІБ, телефони, адреси, суми | 🔴 PII + продакшн-дані | synthetic | 3 вигадані рядки з крайовими випадками валідації |
| Комерційна частина | оцінка, ставка, собівартість, маржа, конкурент, дедлайн | 🟡 внутрішня комерція | узагальнення | `<INTERNAL>` |

**Залишкова категорія документа — 🟡:** назви систем клієнта (CRM, Slack,
Airtable, Postgres, Google Sheets) і схема інтеграції. За розділом 5 чек-ліста
цей документ можна відправляти лише в Claude Team або через API з комерційними
умовами. (Прогони промпту `n8n-integration-spec` на ньому робились на
особистому плані — прийнятно лише тому, що дані синтетичні.)

**Внутрішній документ агенції.** Підготував: акаунт-менеджер, 2026-09-03.

## Контакт з боку клієнта

- [CONTACT_1], керівник операційного відділу — приймає роботу з боку клієнта
- Email: `[CONTACT_1_EMAIL]`
- Телефон: `+380 (XX) XXX-XX-XX`
- Особистий месенджер: `<REMOVED>` — для задачі не потрібен

## Задача

[CLIENT_1] — логістична компанія — вручну обробляє заявки на перевезення:
менеджер копіює дані з пошти в Google Sheets, потім створює запис у CRM і
повідомляє водія в Slack. Треба автоматизувати: заявка → валідація → запис у
CRM → сповіщення водія.
Обсяг: ~400 заявок/тиждень, пікові дні — понеділок і четвер.

Критерії приймання:
- заявка з некоректним телефоном або без адреси доставки не потрапляє в CRM;
- сповіщення водію йде не пізніше ніж за 2 хвилини після заявки;
- усі відхилені заявки видно в окремому звіті.

## Доступи

Жодних значень у документі й у промпті. Агент/розробник отримує їх лише через
змінні середовища або n8n credentials на етапі запуску.

| Сервіс | Роль у задачі | Змінна / credential | Значення |
|---|---|---|---|
| CRM API | створення запису заявки (з опису задачі) | `CRM_API_KEY` | `<SECRET_OUT_OF_BAND>` |
| Slack Bot | сповіщення водія (з опису задачі) | `SLACK_BOT_TOKEN` | `<SECRET_OUT_OF_BAND>` |
| Airtable | **у брифі не вказано** | `AIRTABLE_BASE_ID`, `AIRTABLE_PAT` | `<SECRET_OUT_OF_BAND>` |
| Вхідний webhook (прод) | у брифі не вказано; `ПРИПУЩЕННЯ агенції:` прийом заявок (за назвою вебхука) | `INTAKE_WEBHOOK_URL` | `<SECRET_OUT_OF_BAND>` |
| Postgres (репліка) | **у брифі не вказано** | `ORDERS_DB_URL` | `<SECRET_OUT_OF_BAND>` |

Колонка «Роль у задачі» — лише те, що випливає з опису задачі. Де клієнт роль
не назвав, так і написано; `ПРИПУЩЕННЯ агенції` — наш висновок із прихованого
значення, а не факт від клієнта.

## Приклад даних заявок (синтетичні рядки)

Формат збігається з вигрузкою з продакшену; значення вигадані. Код оператора
`(00)` — маркер вигаданого номера: за форматом номер коректний. Рядки 2 і 3
навмисно показують випадки, які валідація має відхилити.

| # | Замовник | Телефон | Адреса | Сума, ₴ |
|---|---|---|---|---|
| 1 | [CUSTOMER_1] | +380 (00) 000-00-01 | м. Тестове, вул. Прикладна 1, кв. 1 | 5 000 |
| 2 | [CUSTOMER_2] | +380 (00) 000-0 | м. Тестове, просп. Зразковий 2 | 12 000 |
| 3 | ФОП [CUSTOMER_3] | +380 (00) 000-00-03 | _(порожньо)_ | 7 000 |

## Комерційна частина

- Оцінка обсягу робіт: `<INTERNAL>`
- Ставка, собівартість, маржа: `<INTERNAL>`
- Контекст продажу й дедлайн пропозиції: `<INTERNAL>`
