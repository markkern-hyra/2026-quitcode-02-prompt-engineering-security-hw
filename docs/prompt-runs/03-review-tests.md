# Прогін 03 — `review-tests`

Промпт: [`prompts/review-tests.md`](../../prompts/review-tests.md). Методика —
[README](./README.md). Від коміту `9ad3fd6` (після виправлення
`splitInstallments`), паралельно з прогонами 04 і 05a.

| | |
|---|---|
| Ходів / вартість / час | 40 / $1.30 / 211 с |
| Виклики | 19 Bash, 9 Edit (тимчасові мутації), 5 Read |
| Змінені файли після прогону | жодного — `git status` чистий (перевірено незалежно) |
| Відмови в дозволах | 0 |

## Мутації (зі звіту агента)

| # | Функція | Мутація | Впали тести? |
|---|---|---|---|
| 1 | `estimateTotalCents` | `Math.round` → `Math.floor` | так |
| 2 | `estimateTotalCents` | прибрати знижку | так |
| 3 | `estimateTotalCents` | `gross - discount` → `gross + discount` | так |
| 4 | `splitInstallments` | залишок на **останні** платежі замість перших | так (лише тест `[34, 33, 33]`) |
| 5 | `splitInstallments` | `Math.floor` → `Math.ceil` | так |
| 6 | `splitInstallments` | не додавати залишок | так |
| 7 | `formatMoney` | `padStart(2, "0")` → `padStart(1, "0")` | так |
| 8 | `formatMoney` | прибрати знак | так |
| 9 | `formatMoney` | `Math.floor` → `Math.ceil` | так |

Mutation score: **9/9**.

## Знахідки агента і їх перевірка

| Знахідка | Справжня? |
|---|---|
| LOW: назва «коректно округлює при знижці 33.33...%», хоча знижка — рівно 33 % | ✅ так |
| LOW: коментар-«міркування вголос» у тесті на половину цента | ✅ так, але пояснення агента саме помилкове: «`Math.round(0.5) = 0` (banker's rounding у деяких контекстах)» — у JS `Math.round(0.5)` завжди 1 |
| INFO: кейс `(100, 3)` дублюється | ✅ так; **пропущено**, що `(10, 3)` і `(1, 3)` дублюються так само |
| INFO: тест «повертає масив з parts елементів» перевіряє лише довжину | ✅ так |
| INFO: `Number.isInteger` тавтологічний для `Math.round` | ⚠️ спірно — корисний як регресія, агент сам це визнав |

Незалежні розрахунки очікуваних значень — 3 (45000, `[34, 33, 33]`,
`"$1,234,567.89"`) ✅.

## Перевірка acceptance criteria

| Критерій | |
|---|---|
| ≥8 мутацій, ≥2 на функцію, для кожної — впали/який тест | ✅ 9, по 3 |
| Для мутацій, що вижили, — тест, який їх уб'є | ➖ не перевірено: вижилих не було |
| Оманливі тести з `файл:рядок` | ✅ (з одним пропуском дублікатів) |
| ≥3 незалежні розрахунки очікувань | ✅ |
| `git status` чистий | ✅ |
| Без вигаданих зауважень | ✅ |

## Висновок

Промпт спрацював: знайшов реальні дефекти тестів, які лишились після прогону
01 (їх свідомо не правили руками). Але 9/9 на дев'яти «очевидних» мутаціях
більше каже про розмір модуля, ніж про силу тестів: критерій про вижилі мутації
на цій цілі так і не був перевірений. Для більшого модуля варто вимагати
частину мутацій на межових умовах (`<` ↔ `<=`, off-by-one).
