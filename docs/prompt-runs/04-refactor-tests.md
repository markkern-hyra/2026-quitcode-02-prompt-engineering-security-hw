# Прогін 04 — `refactor-tests`

Промпт: [`prompts/refactor-tests.md`](../../prompts/refactor-tests.md). Методика —
[README](./README.md). Від коміту `9ad3fd6`, паралельно з 03 і 05a.

| | |
|---|---|
| Ходів / вартість / час | 81 / $2.39 / 351 с |
| Відмови в дозволах | 7 — комбіновані команди `sed -i.bak '…' src/quote.ts && npm test \| …` для мутацій; агент перейшов на `Edit` |
| Змінені файли | лише `app/src/quote.test.ts` ✅ |
| `npm test` / typecheck (незалежно) | ✅ 34 passed / ✅ |

## Метрики

| | До | Після |
|---|---|---|
| Рядків у файлі | 204 | 70 |
| Тестів у `npm test` | 41 | 34 |
| Мутацій зловлено | 6/6 | 6/6 |

Таблиця мутацій до/після (зі звіту агента): `Math.round` → `Math.floor`;
`gross - discount` → `+`; `i < remainder` → `<=`; `Math.floor` → `Math.ceil` у
`splitInstallments`; `padStart(2)` → `padStart(1)`; прибрати знак — кожна
ловиться і до, і після.

## Незалежна перевірка «жодна перевірка не втрачена»

Звірено вручну всі пари «вхід → очікування» у файлі до й після:

- `estimateTotalCents`: 9 → 9 ✅
- `formatMoney`: 10 → 10 ✅
- `splitInstallments`: усі 15 різних входів збережені ✅. Різниця 41 → 34 —
  рівно 7 злитих дублікатів: `(100, 3)` ×4, `(0, 5)` ×2, `(10, 3)` ×2,
  `(1, 3)` ×2, `(100, 4)` ×2.
- Оманлива назва «33.33...%» і коментарі-«міркування вголос» зникли ✅.

## Порушення

Промпт: «Не додавай нових перевірок». Агент **додав**: точні очікування
`(10, 3)` → `[4, 3, 3]` і `(1, 3)` → `[1, 0, 0]` (раніше перевірялась лише
сума), а кожен рядок таблиці `splitInstallments` тепер перевіряє ще й довжину,
суму, цілі значення та `max − min ≤ 1`. Тести від цього сильніші, але це саме
те, що рефакторинг робити не мав: зміна того, *що* перевіряється.

Жоден критерій цього не ловить — «кожна пара лишилась» і «файл коротший»
виконуються й при додаванні перевірок. Чесніше формулювання для наступної
версії: «кількість `expect` на кожен вхід не зросла».

## Рішення

Диф узято на гілку без правок (коміт `6617b3c`): додані перевірки коректні й
не змінюють продакшн-код. Порушення зафіксоване тут і в розділі «Прогони»
промпту. Повторний прогін ($2.39) заради нешкідливого відхилення визнали
недоцільним.
