# Сирий вихід прогону `e5-cmd-sanitize-check-wrapper`

> Згенеровано з stream-json логу headless-сесії без змін змісту. Редакція: фейковий ключ → `<FAKE_KEY>`, локальні шляхи → відносні шляхи в репо / `<scratchpad>` / `~`. Команди довші за 300 символів обрізано з позначкою `…[обрізано]`, багаторядкові — `…[+N рядк.]`; повні — у stream-json логах.

| Поле | Значення |
|---|---|
| Модель | `claude-opus-4-5-20251101` |
| Режим дозволів | `default` |
| Результат | `success` · ходів 3 · $0.13 · 27 с · відмов у дозволах: 0 |

## Запит

````text
/sanitize-check materials/client-brief.md
````

## Виклики інструментів (підряд однакові згорнуто)

```text
1× Read: docs/sanitization-checklist.md
1× Bash: bash docs/tools/sensitive-lines.sh materials/client-brief.md
```

## Фінальна відповідь агента

## Результати перевірки `materials/client-brief.md`

### 1. Таблиця знахідок (вивід скрипта)

| категорія | рядки | кількість |
|---|---|---|
| секрети | 37,38,39,41 | 4 |
| connection strings | 41 | 1 |
| вебхуки | 40 | 1 |
| email | 17,41 | 2 |
| телефони UA | 18,47,48,49 | 4 |
| хендли месенджерів | 19 | 1 |

*(рядки з `<SECRET_OUT_OF_BAND>`, `(XX)`, `(00)` не рахуються)*

### 2. Вердикт

**Найвища категорія:** 🔴 (секрети, connection strings, вебхук, PII — email, телефони, хендли)

**Куди можна:** **нікуди** в поточному вигляді — файл містить дані категорії 🔴.

**Що зробити перед відправкою:**
- Секрети (рр. 37–41): замінити на `<SECRET_OUT_OF_BAND>` + назва змінної
- Email (рр. 17, 41): `<REMOVED>` або `[CONTACT_1_EMAIL]`
- Телефони (рр. 18, 47–49): маска `+380 (XX) XXX-XX-XX` або синтетика з `(00)`
- Хендл (р. 19): `<REMOVED>` або `[CONTACT_1_MESSENGER]`
- Вебхук (р. 40): `<SECRET_OUT_OF_BAND>`
- Connection string (р. 41): `<SECRET_OUT_OF_BAND>`

Після санітизації 🔴 → перевірити, чи лишились 🟡-дані; обрати інструмент за розділом 5 чек-ліста.

### 3. Чек-ліст ручного проходу (те, чого скрипт не бачить)

Автоматичний крок **не** виявляє:
- імена (ПІБ контактів)
- назви компаній (клієнт, партнери, конкуренти)
- адреси (поштові, офісні)
- комерційну інформацію (ставки, маржа, собівартість, умови угоди)
- рядки, де плейсхолдери вже проставлені (вони відфільтровані)

**Людина має перевірити вручну:**
- [ ] Прочитати документ повністю й кожному блоку поставити 🔴/🟡/🟢
- [ ] Немає жодного справжнього ПІБ, назви клієнта, адреси
- [ ] Немає ставок, собівартості, маржі, згадок про конкурентів
- [ ] Вигрузки замінені синтетикою
- [ ] Секрети — лише `<SECRET_OUT_OF_BAND>` + назва змінної
- [ ] Задача й критерії приймання зрозумілі без оригіналу
- [ ] Обраний інструмент відповідає найвищій категорії, що лишилась у документі
