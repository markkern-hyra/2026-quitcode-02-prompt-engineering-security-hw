# Сирий вихід прогону `a4-review-tests`

> Згенеровано з stream-json логу headless-сесії без змін змісту. Редакція: фейковий ключ → `<FAKE_KEY>`, локальні шляхи → відносні шляхи в репо / `<scratchpad>` / `~`.

| Поле | Значення |
|---|---|
| Модель | `claude-opus-4-5-20251101` |
| Режим дозволів | `acceptEdits` |
| Результат | `success` · ходів 40 · $1.3 · 211 с · відмов у дозволах: 0 |

## Запит

````text

# Рев'ю тестів: чи ловлять вони дефекти

## Роль

Рев'юер тестів, який не довіряє зеленому CI: перевіряє, що тести падають, коли
код зламано, а не лише проходять, коли він цілий.

## Мета

Оцінити, чи `app/src/quote.test.ts` справді захищає `app/src/quote.ts`: знайти
мутації коду, які тести пропускають, і дефекти в самих тестах.

## Контекст

- Код: `app/src/quote.ts` — три функції; усі суми — цілі центи; залишок у
  `splitInstallments` іде на перші платежі.
- Тести: `app/src/quote.test.ts` — частину згенеровано AI-агентом.
- Команда: `cd app && npm test`.

## Обмеження

- Мутації вноси тимчасово, **по одній**, і після кожної повертай файл у
  вихідний стан (`git checkout -- app/src/quote.ts`). Наприкінці `git status`
  має бути чистим.
- Не виправляй ні код, ні тести — результат цього промпту лише звіт.
- Не коментуй форматування. Назви й коментарі — лише якщо вони вводять в оману
  (не відповідають тому, що перевіряє тест).
- Максимум 10 знахідок, відсортованих за серйозністю.

## Acceptance criteria

- [ ] Застосовано щонайменше 8 мутацій, мінімум 2 на кожну функцію (напр.
      `Math.round` → `Math.floor`, прибрати знижку, зламати розподіл
      залишку, змінити `padStart`, знак); для кожної записано, чи впав хоч
      один тест (так/ні) і який.
- [ ] Для кожної мутації, що вижила, запропоновано конкретний тест: вхід і
      очікуваний результат, який її вб'є.
- [ ] Названо тести, що вводять в оману: назва чи коментар не відповідає
      перевірці, дублікати, тавтологічні перевірки — з `файл:рядок`.
- [ ] Для щонайменше 3 очікуваних значень у тестах показано незалежний
      розрахунок (перевірка, що очікування не «підігнане» під реалізацію).
- [ ] Наприкінці `git status` чистий — жодна мутація не лишилась.
- [ ] Якщо проблем немає — так і написано, без вигаданих зауважень.

## Формат відповіді

1. Таблиця мутацій: `# | функція | мутація | тести впали? | який тест зловив`.
2. Знахідки: `СЕРЙОЗНІСТЬ | файл:рядок | проблема | пропозиція`.
3. Підсумок: mutation score (вбиті / усього) і 1–3 найважливіші дії.

## Stop

Після таблиці мутацій і знахідок зупинись. Тести й код не виправляй — це
окремий запит.
````

## Виклики інструментів (підряд однакові згорнуто)

```text
1× Read: app/src/quote.ts
1× Read: app/src/quote.test.ts
1× Read: AGENTS.md
2× TodoWrite: null
1× Bash: cd app && npm test
1× Edit: app/src/quote.ts
1× Bash: npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× Read: app/src/quote.ts
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× TodoWrite: null
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× TodoWrite: null
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts
1× Edit: app/src/quote.ts
1× Bash: cd app && npm test 2>&1 || true
1× Bash: cd <worktree> && git checkout -- app/src/quote.ts && git status
1× TodoWrite: null
1× Read: app/src/quote.test.ts
1× TodoWrite: null
```

## Фінальна відповідь агента

## Звіт: Рев'ю тестів

### 1. Таблиця мутацій

| # | Функція | Мутація | Тести впали? | Який тест зловив |
|---|---------|---------|--------------|------------------|
| 1 | `estimateTotalCents` | `Math.round` → `Math.floor` | Так | "округлює знижку, що дає половину цента (0.5)" |
| 2 | `estimateTotalCents` | Прибрати знижку (`discount = 0`) | Так | "застосовує знижку", "повертає 0 при 100% знижці", "коректно округлює при знижці 33.33...%" |
| 3 | `estimateTotalCents` | `gross - discount` → `gross + discount` | Так | "застосовує знижку", "повертає 0 при 100% знижці", "округлює знижку, що дає половину цента (0.5)", "коректно округлює при знижці 33.33...%" |
| 4 | `splitInstallments` | `i < remainder` → `i >= parts - remainder` | Так | "$1.00 на 3 платежі: [34, 33, 33], сума = 100" |
| 5 | `splitInstallments` | `Math.floor` → `Math.ceil` | Так | 11 тестів (усі тести на збереження суми) |
| 6 | `splitInstallments` | Прибрати `+ (i < remainder ? 1 : 0)` | Так | 11 тестів (усі тести на збереження суми) |
| 7 | `formatMoney` | `padStart(2, "0")` → `padStart(1, "0")` | Так | "форматує 0 центів", "форматує 1 цент", "форматує 100 центів як $1.00", та ін. |
| 8 | `formatMoney` | Прибрати знак (`sign = ""`) | Так | "форматує від'ємні значення", "результат коректний для від'ємного 1 цента" |
| 9 | `formatMoney` | `Math.floor` → `Math.ceil` | Так | "форматує центи", "форматує від'ємні значення", "форматує 1 цент", та ін. |

### 2. Знахідки

| Серйозність | Файл:рядок | Проблема | Пропозиція |
|-------------|------------|----------|------------|
| LOW | `quote.test.ts:46-51` | Коментар у тесті "округлює знижку, що дає половину цента (0.5)" містить помилку: написано `round → 1`, але `Math.round(0.5) = 0` (banker's rounding у деяких контекстах) або `= 1` (стандартна поведінка JS). Коментар незрозуміло пояснює очікування | Спростити коментар: `// gross=1, discount=0.5 → gross - discount = 0.5 → Math.round(0.5) = 1` |
| LOW | `quote.test.ts:66-70` | Назва тесту "коректно округлює при знижці 33.33...%" не відповідає реальності: `discountPercent: 33` — це ціле число 33%, а не 33.33...% | Змінити назву на "коректно обчислює при знижці 33%" або тестувати дробову знижку |
| INFO | `quote.test.ts:99-105`, `quote.test.ts:150` | Тест "зберігає суму при неподільному залишку (100, 3)" дублюється: той самий кейс присутній у параметризованому тесті на рядку 150 | Видалити дублікат або використовувати лише параметризований варіант |
| INFO | `quote.test.ts:79-82` | Тест "повертає масив з parts елементів" слабкий: не перевіряє конкретні значення, лише довжину — мутація, що повертає `[0,0,0,0]`, пройде | Додати перевірку суми або конкретних значень |
| INFO | `quote.test.ts:54-57` | Тест "результат є цілим числом" — тавтологічний для коректної реалізації: `Math.round()` завжди повертає ціле | Тест корисний як регресія, але не ловить логічних помилок |

### 3. Незалежна перевірка очікуваних значень

**Тест: `estimateTotalCents({ hours: 10, rateCents: 5000, discountPercent: 10 })` → 45000**
- gross = 10 × 5000 = 50000
- discount = 50000 × 10 / 100 = 5000
- result = 50000 - 5000 = 45000 ✓

**Тест: `splitInstallments(100, 3)` → `[34, 33, 33]`**
- base = floor(100 / 3) = 33
- remainder = 100 - 33×3 = 100 - 99 = 1
- Перші `remainder` (=1) елементів отримують +1: `[34, 33, 33]` ✓
- Сума: 34 + 33 + 33 = 100 ✓

**Тест: `formatMoney(123456789)` → "$1,234,567.89"**
- whole = floor(123456789 / 100) = 1234567
- frac = 123456789 % 100 = 89 → padStart(2, "0") = "89"
- 1234567 з locale = "1,234,567"
- Результат: "$1,234,567.89" ✓

### 4. Підсумок

**Mutation Score: 9/9 = 100%** — усі мутації вбиті.

**Найважливіші дії:**
1. Виправити коментар/назву тесту на рядку 66 — вводить в оману щодо тестованого значення (33 ≠ 33.33%)
2. Видалити дублікат тестового кейсу `(100, 3)` (рядки 99-105 та 150)
3. Посилити тест "повертає масив з parts елементів" (рядок 79-82) — додати перевірку суми
