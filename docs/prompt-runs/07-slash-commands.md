# Прогін 07 — slash-команди (Task D)

Команди: [`.claude/commands/add-tests.md`](../../.claude/commands/add-tests.md),
[`.claude/commands/sanitize-check.md`](../../.claude/commands/sanitize-check.md).
Методика — [README](./README.md). Обидва прогони — `claude -p "/<команда>
<аргумент>"`, `--permission-mode default` **без** allowlist: дозволи дає лише
`allowed-tools` із frontmatter самої команди.

## `/add-tests app/src/quote.ts`

Від **стартового коміту `da25af6`** (вихідний код із вадою, 4 базові тести);
файл команди підкладено в одноразовий worktree.

| | |
|---|---|
| Команда розпізнана | ✅ `slash_commands` в init містить `add-tests`, `$ARGUMENTS` підставлено |
| Ходів / вартість / час | 14 / $0.49 / 98 с |
| Відмови в дозволах | 0 — вистачило `allowed-tools` команди |
| Змінені файли | лише `app/src/quote.test.ts` (+191) |
| `npm test` / typecheck (незалежно) | ✅ 55 passed \| 14 expected fail / ✅ |
| `.only` / `.skip` / `console.log` | немає |
| Знайдена вада | ✅ `splitInstallments`: `(100, 3)` → 99, `(10, 3)` → 9, `(1, 3)` → 0, `(10000, 7)` → 10003 — у `it.fails` з `// ВАДА:` |

Неточність у звіті: у «Відкритих питаннях» сказано, що від'ємний `parts`
повертає `[]`, а на цьому коді він кидає `RangeError: Invalid array length` —
твердження без запуску.

Висновок: команда відтворює результат промпту `add-tests` v2 на вихідному коді
без жодних підказок, окрім власного тексту й аргументу.

## `/sanitize-check materials/client-brief.md`

Від коміту `6d6e30a`. Навмисно — на **сирому** брифі: це саме той випадок, коли
модель не повинна бачити вмісту.

| | |
|---|---|
| Команда розпізнана | ✅ |
| Ходів / вартість / час | 14 / $0.31 / 56 с |
| Відмови в дозволах | 0 |
| Що прочитав інструментом Read | лише `docs/sanitization-checklist.md` — **сам бриф не відкривав** |
| Команди | 6 × `grep -nEi '…' файл \| cut -d: -f1 \| paste -sd, -` + 6 × `grep -cEi` |
| Вміст рядків брифу у виводі команд | **немає** — перевірено: вивід кожної з 12 команд складається лише з цифр і ком |

Результат команди:

| Категорія | Рядки |
|---|---|
| Секрети | 37, 38, 39, 41 |
| Connection strings | 41 |
| Вебхуки | 40 |
| Email | 17, 41 |
| Телефони UA | 18, 47, 48, 49 |
| Хендли месенджерів | 19 |

Вердикт агента: «Найвища категорія: 🔴 … Куди можна в поточному вигляді:
**нікуди** — ні в публічну модель, ні в Claude Team, ні в API» + чек-лист
ручного проходу (імена, назви компаній, адреси, комерція — «те, чого grep не
бачить»). Збігається з ручною класифікацією з Task B: ПІБ контакту (рядок 16),
назва клієнта й комерційна частина автоматично не знайдені — і команда прямо
про це попереджає.

Застереження: заборона відкривати файл — текстова. Read за замовчуванням
дозволений, і `allowed-tools` команди не може його заборонити для одного файлу;
у цьому прогоні модель правило виконала, але гарантію дав би лише `deny` у
налаштуваннях (як для `.env` у Task C).
