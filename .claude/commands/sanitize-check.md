---
description: Перевірити файл на чутливі дані перед відправкою в AI — лише номери рядків і категорії, без показу значень
argument-hint: <шлях до файлу, напр. materials/client-brief.md>
allowed-tools: Read(docs/sanitization-checklist.md), Bash(grep:*), Bash(cut:*), Bash(paste:*), Bash(wc:*)
---

# Перевірка `$ARGUMENTS` на чутливі дані перед AI

Джерело правил: `docs/sanitization-checklist.md` (розділи 1, 4, 5).

## Роль

Інженер з безпеки даних агенції, який перевіряє документ клієнта **не
дивлячись на значення**: сама модель — це те місце, куди чутливе потрапляти не
повинно.

## Мета

Визначити, чи можна `$ARGUMENTS` відправляти в AI-інструмент і в який tier, —
назвавши рядки й категорії знахідок, але не розкриваючи жодного значення.

## Контекст

- Файл для перевірки: `$ARGUMENTS`.
- Класифікація (🔴/🟡/🟢) і «інструмент під категорію» — у
  `docs/sanitization-checklist.md`, прочитай його першим.
- Патерни для автоматичного кроку (усі з `grep -nEi`):
  - секрети: `(api[_-]?key|token|secret|passw|bearer|\bpat[A-Za-z0-9]*\.|xox[abprs]-|\bsk-|\bfake_|\bapp[A-Za-z0-9]{10,}|://[^ /:]+:[^ @]+@)`
  - connection strings: `(postgres(ql)?|mysql|mongodb(\+srv)?|redis|amqp)://`
  - вебхуки: `/webhook/`
  - email: `[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}`
  - телефони UA (міжнародний і локальний формат): `(\+?380[ ()-]*[0-9]{2}[ )-]*[0-9]{3}|\b0[0-9]{2}[ )-]*[0-9]{3}[ -]?[0-9]{2}[ -]?[0-9]{2}\b)`
  - хендли месенджерів: `((^|[ (])@[A-Za-z0-9_]{5,}|t\.me/)`

## Обмеження

- **Не відкривай `$ARGUMENTS` інструментом Read і не виводь його рядків.**
  Кожен `grep` по цьому файлу — лише з `| cut -d: -f1` (номери рядків) або з
  `-c` (кількість). Вміст рядків не повинен з'явитися у виводі жодної команди.
- Не маскуй і не змінюй файл — це окремий крок, який робить людина.
- Жодних інших команд, окрім `grep`, `cut`, `paste`, `wc`.

## Acceptance criteria

- [ ] По кожній із 6 категорій запущено `grep` і вказано номери рядків (або
      «не знайдено»).
- [ ] У виводі жодної виконаної команди немає вмісту рядків `$ARGUMENTS`.
- [ ] Названо найвищу знайдену категорію і, за розділом 5 чек-ліста, куди файл
      можна відправляти в поточному вигляді.
- [ ] Окремо сказано, що автоматичний крок **не** бачить імен, назв компаній,
      адрес і комерції, — з переліком того, що людина має перевірити вручну.

## Формат відповіді

1. Таблиця `категорія | рядки | кількість`.
2. Вердикт: найвища категорія → куди можна → що зробити перед відправкою.
3. Чек-ліст ручного проходу (розділ 4, крок 2).

## Stop

Після вердикту зупинись. Файл не відкривай і не виправляй, навіть якщо
знайдено 🔴, — маскування робить людина за чек-лістом.
